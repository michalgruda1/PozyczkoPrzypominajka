@page
@model PozyczkoPrzypominajkaV2.Pages.Loans.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Loan[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Loan[0].Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Loan[0].RepaymentDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Loan[0].RepaymentAmount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Loan) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RepaymentDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RepaymentAmount)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.LoanID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.LoanID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.LoanID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
